---
import Footer from '../components/Footer.astro';
import FloatingWhatsApp from '../components/FloatingWhatsApp.astro';
import '../styles/global.css';

const PAYMENT_URL = 'https://secure.cardcom.solutions/EA/EA5/VEmNO09CUOPMpERwwew/PaymentSP';

const dates = ['6.1', '13.1', '20.1', '27.1', '3.2', '10.2'];

const modules = [
  {
    title: 'זוגיות, דייטים וחדר המיטות',
    text: 'קריאת אותות משיכה, קרבה ואמת במרחב הכי אינטימי שיש - מפגישה ראשונה ועד חדר השינה. מה הגוף מגלה כשהמילים משתיקות.',
  },
  {
    title: 'אלימות והפרעות אישיות',
    text: 'שפת גוף שמנבאת אלימות ומאפיינים גופניים של הפרעות אישיות (כולל פסיכופתיה). תלמדו לזהות נורות אדומות ואותות אזהרה עוד לפני שהם הופכים למילים.',
  },
  {
    title: 'סטוריטלינג ועמידה מול קהל',
    text: 'איך מספרים סיפור ומלבישים אותו בשפת גוף שמחזיקה קהל - שני מפגשים ייחודיים בשיתוף השחקן <strong>אמנון וולף</strong>.',
  },
  {
    title: 'זיהוי שקרים ברמה מקצועית',
    text: 'לא עוד סימן אחד או שניים. צלילה מעמיקה לזיהוי מיקרו-ביטויים, חוסר אמת ומניפולציות - הרבה מעבר למה שנלמד בקורס הבסיסי.',
  },
  {
    title: 'משא ומתן וקבלת החלטות',
    text: 'לקרוא את הצד השני בזמן אמת, לדעת מתי לדחוף ומתי לעצור, ולנהל את שפת הגוף שלכם כך שתשרת אתכם סביב השולחן.',
  },
];

const faqs = [
  {
    q: 'האם הקורס מתאים למי שלא סיים את הקורס הבסיסי?',
    a: 'לא. הקורס המתקדם מיועד אך ורק לבוגרי הקורס הבסיסי "ניתוח גוף פתוח" של מעיין בשן - הוא בנוי כהמשך ישיר, לא כמבוא.',
  },
  {
    q: 'מה ההבדל מהקורס הבסיסי?',
    a: 'הקורס הבסיסי מלמד את השפה עצמה מאפס. הקורס המתקדם תוקף את שפת הגוף לפי זוויות - אלימות, זוגיות, שקרים, משא ומתן - עם הרבה יותר תרגול מעשי: מיקרו-הבעות ועמידה מול קהל ומצלמה.',
  },
  {
    q: 'כמה משתתפים בקבוצה?',
    a: 'עד 40 בוגרים בלבד, כדי לשמור על רמת תרגול אישי גבוהה בכל מפגש.',
  },
  {
    q: 'מה אם אפסיד מפגש?',
    a: 'אפשר להשלים בקורס הבא או לקבל גישה להקלטת המפגש שהחסרתם.',
  },
  {
    q: 'מה קורה בכל מפגש בפועל?',
    a: 'שעתיים הרצאה על הנושא של אותו מפגש, ואחריהן שעתיים תרגול מעשי צמוד עם מעיין ופידבק מיידי - מיקרו-הבעות, סימולציות, ועמידה מול קהל ומצלמה. סה"כ 4 שעות למפגש.',
  },
  {
    q: 'מה תנאי הביטול?',
    a: 'ביטול והחזר מלא אפשרי עד 14 יום לפני תחילת הקורס. ביטול מאוחר יותר כרוך בדמי ביטול לפי המדיניות המלאה (פירוט למטה).',
    extra: {
      title: 'נספח – מדיניות ביטול השתתפות בקורס',
      intro: 'משתתף המבקש לבטל את השתתפותו בקורס יודיע על כך בכתב, באמצעות אחת מהדרכים הבאות:',
      contacts: ['WhatsApp: 050-558-5177', 'דוא"ל: maayan.inst@gmail.com'],
      rules: [
        'במקרה של ביטול ההשתתפות עד 14 ימים לפני מועד פתיחת הקורס, יינתן החזר כספי מלא (ללא עלות).',
        'במקרה של ביטול ההשתתפות החל מ־14 ימים ועד 7 ימים לפני מועד פתיחת הקורס, יינתן החזר בשיעור של 50% מעלות הקורס.',
        'במקרה של ביטול ההשתתפות פחות מ־7 ימים לפני פתיחת הקורס, או לאחר תחילת הקורס, לא יינתן החזר כספי (חיוב מלא בעלות הקורס).',
      ],
      outro: 'מועד קבלת הודעת הביטול בכתב אצל מפעילת הקורס הוא שייחשב כמועד הביטול לצורך חישוב הזכאות להחזר. הוראות נספח זה יחולו בכפוף להוראות כל דין, ובכל מקרה שבו קיימת לצרכן זכות שאינה ניתנת להתניה לפי חוק, יחולו הוראות הדין המחייבות.',
    },
  },
];
---
<!doctype html>
<html lang="he" dir="rtl">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>שפת גוף - קורס מתקדמים | מעיין בשן</title>
    <meta name="description" content='קורס המשך בלעדי לבוגרי "ניתוח גוף פתוח" - 6 מפגשי העשרה ותרגול מעשי אינטנסיבי עם מעיין בשן. אלימות, זוגיות, שקרים ומשא ומתן.' />
    <meta name="keywords" content="מעיין בשן, מעין בשן, שפת גוף, שפתגוף, קורס מתקדמים, ניתוח גוף פתוח" />
    <meta name="google-site-verification" content="eP1EVt4zfY7kOhKGsSnLYR49MPqn7bKNlRavQqKDkGo" />
    <link rel="canonical" href="https://maayanbashan.co.il/body-language-advanced-course" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Assistant:wght@400;500;600;700;800&display=swap" rel="stylesheet" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content="שפת גוף - קורס מתקדמים | מעיין בשן" />
    <meta property="og:description" content='קורס המשך בלעדי לבוגרי "ניתוח גוף פתוח" - 6 מפגשים, ינואר-פברואר 2027, הרצליה' />
    <meta property="og:url" content="https://maayanbashan.co.il/body-language-advanced-course" />
    <meta property="og:image" content="https://maayanbashan.co.il/images/maayan-hero.png" />
    <meta property="og:locale" content="he_IL" />
    <meta property="og:site_name" content="מעיין בשן" />
    <meta name="twitter:card" content="summary_large_image" />

    <!-- Structured data: Course -->
    <script type="application/ld+json" set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'Course',
      name: 'שפת גוף - קורס מתקדמים',
      description: 'קורס המשך לבוגרי "ניתוח גוף פתוח" - 6 מפגשי העשרה ותרגול מעשי: אלימות והפרעות אישיות, זוגיות ודייטים, סטוריטלינג, שקרים, משא ומתן.',
      provider: {
        '@type': 'Person',
        name: 'מעיין בשן',
        sameAs: 'https://maayanbashan.co.il',
      },
      offers: {
        '@type': 'Offer',
        price: '3800',
        priceCurrency: 'ILS',
        availability: 'https://schema.org/InStock',
        url: 'https://maayanbashan.co.il/body-language-advanced-course',
      },
      hasCourseInstance: {
        '@type': 'CourseInstance',
        courseMode: 'Onsite',
        location: {
          '@type': 'Place',
          name: 'אשכול פיס, הרצליה',
        },
        startDate: '2027-01-06',
      },
    })} />
  </head>
  <body>
    <nav class="ac-nav">
      <a href="/" class="ac-logo"><img src="/images/logo-white.png" alt="מעיין בשן" /></a>
      <a href="#register" class="ac-nav-cta">הרשמה לקורס</a>
    </nav>

    <!-- TOP STRIP -->
    <div class="ac-top-strip">
      <div class="ac-badge">🎓 רק לבוגרים שלנו</div>
      <div class="ac-eyebrow-pill">✦ קורס מתקדמים בשפת גוף · ינואר 2027</div>
    </div>

    <!-- PRACTICE EMPHASIS -->
    <section class="ac-section ac-practice-section reveal">
      <svg class="ac-motif ac-motif-body" viewBox="0 0 120 200" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M60 15a15 15 0 1 1 0 30 15 15 0 0 1 0-30Z" stroke="currentColor" stroke-width="1" opacity="0.5"/>
        <path d="M60 45v60M35 70c8-6 17-9 25-9s17 3 25 9M40 105h40M45 105l-8 60M75 105l8 60" stroke="currentColor" stroke-width="1" opacity="0.5"/>
        <circle cx="60" cy="45" r="2.5" fill="currentColor" opacity="0.7"/>
        <circle cx="35" cy="70" r="2.5" fill="currentColor" opacity="0.7"/>
        <circle cx="85" cy="70" r="2.5" fill="currentColor" opacity="0.7"/>
      </svg>
      <svg class="ac-motif ac-motif-speech" viewBox="0 0 140 90" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M15 15h90a10 10 0 0 1 10 10v35a10 10 0 0 1-10 10H55l-20 18v-18H15A10 10 0 0 1 5 60V25A10 10 0 0 1 15 15Z" stroke="currentColor" stroke-width="1" opacity="0.5"/>
        <circle cx="40" cy="42" r="2.5" fill="currentColor" opacity="0.7"/>
        <circle cx="65" cy="42" r="2.5" fill="currentColor" opacity="0.7"/>
        <circle cx="90" cy="42" r="2.5" fill="currentColor" opacity="0.7"/>
      </svg>
      <div class="ac-inner">
        <div class="ac-practice-top">
          <div class="ac-practice-text">
            <h2>גם ידע מתקדם.<br /><em class="ac-marker">גם יכולת ליישם</em>.</h2>
            <p class="ac-practice-lead">
              הכוח של קורס למתקדמים טמון בשילוב המדויק בין התרגול לתאוריה - בכל מפגש צוללים לידע תיאורטי ברמה הגבוהה ביותר, ומייד עולים לתרגל אותו בשטח, ממש באותו ערב. זה לא מבנה של "קודם לומדים, ואולי יום אחד נתרגל" - זה מבנה שחוזר על עצמו בכל אחד משישה המפגשים: תיאוריה עמוקה, ואז תרגול מיידי על אותו החומר, מול המצלמה, מול הקבוצה, ועם פידבק אישי ומדויק של מעיין על כל תזוזה ומיקרו-הבעה. כך יוצאים מכל מפגש בנפרד - לא רק בסוף הקורס - גם עם תובנות עמוקות וחדשות, וגם עם מיומנות פרקטית שמשתמשים בה כבר למחרת בבוקר.
            </p>
            <p class="ac-practice-note">התהליך הזה - לומדים, מתרגלים, יוצאים עם יכולת - חוזר בכל אחד משישה המפגשים, לא רק פעם אחת בקורס.</p>
          </div>
          <div class="ac-practice-photo">
            <span class="ac-practice-photo-frame" aria-hidden="true"></span>
            <img src="/images/maayan-hero.png" alt="מעיין בשן" />
          </div>
        </div>
        <div class="ac-practice-grid">
          <div class="ac-practice-card">
            <span class="ac-practice-num">01</span>
            <h3>לומדים</h3>
            <p>שעתיים הרצאת עומק על שפת גוף מזווית אחרת בחיים - ידע חדש שלא נלמד בקורס הבסיסי</p>
          </div>
          <div class="ac-practice-arrow" aria-hidden="true">←</div>
          <div class="ac-practice-card">
            <span class="ac-practice-num">02</span>
            <h3>מתרגלים</h3>
            <p>שעתיים תרגול מעשי אינטנסיבי - מיקרו-הבעות, סימולציות, ניתוח בזמן אמת ועמידה מול קהל ומצלמה</p>
          </div>
          <div class="ac-practice-arrow" aria-hidden="true">←</div>
          <div class="ac-practice-card ac-practice-card-gold">
            <span class="ac-practice-num">03</span>
            <h3>יוצאים עם יכולת</h3>
            <p>פידבק מדויק ומיידי על כל תרגיל, בכל אחד מששת המפגשים - כדי שהידע לא יישאר על הנייר, אלא יהפוך ליכולת טבעית בשטח</p>
          </div>
        </div>
      </div>
    </section>

    <!-- HERO -->
    <section class="ac-hero">
      <div class="ac-hero-shapes" aria-hidden="true">
        <span class="ac-shape ac-shape-diamond1"></span>
        <span class="ac-shape ac-shape-diamond2"></span>
        <svg class="ac-hero-motif" viewBox="0 0 200 200" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M100 20c-30 0-45 22-45 45 0 12 5 20 5 32 0 18-12 25-12 45 0 20 22 36 52 36s52-16 52-36c0-20-12-27-12-45 0-12 5-20 5-32 0-23-15-45-45-45Z" stroke="currentColor" stroke-width="1" opacity="0.5"/>
          <path d="M100 20v158M70 55c8 6 12 14 12 24M130 55c-8 6-12 14-12 24M62 100c10 4 16 10 18 20M138 100c-10 4-16 10-18 20" stroke="currentColor" stroke-width="1" opacity="0.5"/>
          <circle cx="70" cy="55" r="2.5" fill="currentColor" opacity="0.7"/>
          <circle cx="130" cy="55" r="2.5" fill="currentColor" opacity="0.7"/>
          <circle cx="62" cy="100" r="2.5" fill="currentColor" opacity="0.7"/>
          <circle cx="138" cy="100" r="2.5" fill="currentColor" opacity="0.7"/>
          <circle cx="100" cy="150" r="2.5" fill="currentColor" opacity="0.7"/>
        </svg>
      </div>
      <div class="ac-hero-text ac-hero-text-solo">
        <h1>לקחת את הכלים מהקורס הבסיסי —<br />ולהפוך אותם <em class="ac-marker">למיומנות אוטומטית</em>.</h1>
        <p class="ac-hero-sub">6 מפגשים פרונטליים של 4 שעות - מחצית תיאוריה ומחצית תרגול מעשי בכל מפגש - על הבמה ובזוגות.</p>
        <ul class="ac-bullets">
          <li><strong>מו״מ וזיהוי שקרים:</strong> טכניקות מתקדמות לגילוי חוסר אמת ושליטה בשיחה</li>
          <li><strong>פרופיילינג וסכנה:</strong> זיהוי הפרעות אישיות ונורות אדומות בשפת גוף</li>
          <li><strong>זוגיות ואינטימיות:</strong> פענוח שפת הגוף בחדר המיטות, בדייטים ובתקשורת זוגית</li>
          <li><strong>סטוריטלינג ונוכחות:</strong> עמידה מול קהל והעברת מסר כריזמטי, בשיתוף השחקן אמנון וולף</li>
        </ul>
        <div class="ac-cta-row">
          <a href="#register" class="ac-btn ac-btn-hero">אני רוצה לשריין מקום ←</a>
          <div class="ac-price-tag">
            <span class="ac-price-old">4,400 ₪</span>
            <strong>3,800 ₪ בלבד</strong>
            <span>מתחילים 6.1.27 · אשכול פיס, הרצליה</span>
          </div>
        </div>
      </div>
    </section>

    <!-- TRUST BAR -->
    <div class="ac-trust-bar">
      <div><span>20+</span>שנות ניסיון</div>
      <div><span>40</span>מקומות בלבד</div>
      <div><span>6</span>מפגשי העשרה</div>
      <div><span>1:1</span>יחס תרגול-הרצאה</div>
    </div>

    <!-- MODULES -->
    <section class="ac-section ac-section-alt reveal ac-section-motif">
      <svg class="ac-motif ac-motif-eye" viewBox="0 0 120 60" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M10 30c15-20 45-24 50-24s35 4 50 24c-15 20-45 24-50 24s-35-4-50-24Z" stroke="currentColor" stroke-width="1" opacity="0.5"/>
        <circle cx="60" cy="30" r="12" stroke="currentColor" stroke-width="1" opacity="0.5"/>
        <circle cx="60" cy="30" r="3" fill="currentColor" opacity="0.7"/>
      </svg>
      <div class="ac-inner">
        <div class="ac-eyebrow">מה לומדים</div>
        <h2><em class="ac-marker-soft">המבט ההקפי</em>: קריאת שפת גוף מזוויות מורכבות</h2>
        <p class="ac-section-desc">מיישמים את התיאוריה בפועל: תרגול מזוויות שלא הכרתם.</p>
        <div class="ac-modules">
          {modules.map((m, i) => (
            <div class="ac-module">
              <span class="ac-module-num" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
              <div class="ac-module-body">
                <h3>{m.title}</h3>
                <p set:html={m.text} />
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- GUEST: AMNON WOLF -->
    <section class="ac-section reveal">
      <div class="ac-inner ac-guest-grid">
        <div class="ac-guest-photos">
          <img src="/images/advanced-course/maayan-amnon-piggyback.jpg" alt="מעיין בשן ואמנון וולף" class="ac-guest-photo-main" />
          <img src="/images/advanced-course/maayan-amnon-back-to-back.jpg" alt="מעיין בשן ואמנון וולף" class="ac-guest-photo-accent" />
        </div>
        <div class="ac-guest-text">
          <div class="ac-eyebrow">אורח מיוחד בשני מפגשי הסטוריטלינג</div>
          <h2>מעיין בשן <em class="ac-marker-soft">ואמנון וולף</em></h2>
          <p>שניים מהמפגשים המיוחדים בקורס נעשים בשיתוף השחקן אמנון וולף - איך מספרים סיפור, ואיך מלבישים את המילים בשפת גוף נכונה שתפתח לכם דלתות להשיג כל דבר שתבחרו.</p>
        </div>
      </div>
    </section>

    <!-- SCHEDULE -->
    <section class="ac-section ac-section-alt reveal">
      <div class="ac-inner">
        <div class="ac-eyebrow">לוח זמנים</div>
        <h2>6 <em class="ac-marker-soft">מפגשי עומק</em></h2>
        <p class="ac-section-desc">ימי רביעי · 17:00–21:00 · אשכול פיס, הרצליה · ינואר–פברואר 2027</p>
        <div class="ac-tickets">
          {dates.map((d, i) => (
            <div class="ac-ticket">
              <span class="ac-ticket-day">רביעי</span>
              <span class="ac-ticket-date">{d}</span>
              <span class="ac-ticket-num">מפגש {i + 1}</span>
            </div>
          ))}
        </div>
        <div class="ac-meta-grid">
          <div class="ac-meta-item">
            <span class="ac-emoji">🕘</span>
            <div><h4>שעות</h4><p>17:00 עד 21:00 בכל מפגש</p></div>
          </div>
          <div class="ac-meta-item">
            <span class="ac-emoji">📍</span>
            <div><h4>מיקום</h4><p>אשכול פיס, הרצליה</p></div>
          </div>
          <div class="ac-meta-item">
            <span class="ac-emoji">🎓</span>
            <div><h4>קהל יעד</h4><p>רק לבוגרי הקורס הבסיסי · <strong class="ac-marker-soft">עד 40 משתתפים בלבד!</strong></p></div>
          </div>
        </div>
        <p class="ac-schedule-note">מספר המקומות מוגבל בכוונה - כדי לאפשר תרגול משמעותי לכל משתתף.</p>
      </div>
    </section>

    <!-- PRICE -->
    <section class="ac-section reveal ac-section-motif">
      <svg class="ac-motif ac-motif-brain" viewBox="0 0 120 100" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M30 50c-8-10-4-28 12-32 4-8 16-10 22-2 10-2 20 6 18 16 8 4 10 16 2 22 2 10-8 18-18 14-6 8-20 6-24-2-12 2-22-8-18-18-4-4-4-2 6 2Z" stroke="currentColor" stroke-width="1" opacity="0.5"/>
        <path d="M45 30c2 8 0 16-6 22M65 28c-2 8 2 18 8 22M40 58c6 2 12 2 18-2" stroke="currentColor" stroke-width="1" opacity="0.5"/>
      </svg>
      <div class="ac-inner">
        <div class="ac-eyebrow">מחיר</div>
        <h2><em class="ac-marker-soft">מחיר מיוחד</em> לנרשמים המוקדמים</h2>
        <p class="ac-section-desc">שישה מפגשי עומק מרוכזים, יחס גבוה במיוחד בין הרצאה לתרגול מעשי אישי מול מעיין.</p>
        <div class="ac-early-bird-banner">
          <span class="ac-early-bird-icon">⏳</span>
          <div>
            <strong>המחיר הזה שמור לנרשמים המוקדמים</strong>
            <p>ככל שנרשמים מוקדם יותר - גם המחיר הזה נשמר, וגם המקום מובטח. 40 מקום נשמע הרבה, אבל בקבוצה שמתרגלת אישית מול המצלמה ומקבלת פידבק צמוד מעיין, המקומות נגמרים הרבה יותר מהר משנדמה.</p>
          </div>
        </div>
        <div class="ac-price-grid">
          <div class="ac-price-card">
            <div class="ac-price-label">מחיר מיוחד לבוגרים · לנרשמים המקדימים</div>
            <div class="ac-price-was">מחיר מקורי: 4,400 ₪</div>
            <div class="ac-price-now">3,800 <span>₪</span></div>
            <p>שישה מפגשי עומק, ארבע שעות כל מפגש - הרצאה מעמיקה ותרגול מעשי צמוד עם פידבק מיידי.</p>
            <a href="#register" class="ac-btn">הרשמה עכשיו ←</a>
            <div class="ac-urgency"><span class="ac-urgency-dot"></span> עד 40 בוגרים בלבד - פחות ממה שזה נשמע</div>
          </div>
          <div class="ac-info-card">
            <span class="ac-info-photo"><img src="/images/maayan-hero.png" alt="מעיין בשן" /></span>
            <span class="ac-info-tag">🎓 למי זה מתאים</span>
            <h3>רק לבוגרי הקורס הבסיסי</h3>
            <p>קורס ההמשך בנוי במיוחד לבוגרי "ניתוח גוף פתוח" שרוצים לקחת את הידע צעד אחד קדימה - להפסיק רק לנתח, ולהתחיל ליישם את שפת הגוף בסיטואציות הכי מורכבות: מול קהל, בזוגיות, במשא ומתן ובזיהוי מניפולציות.</p>
            <hr />
            <p class="ac-small">ביטול והחזר מלא עד 14 יום לפני הקורס · החזר של 50% עד 7 ימים לפני. פירוט מלא בשאלות הנפוצות למטה.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- PAYMENT BANNER -->
    <section class="ac-section ac-instant-section reveal">
      <div class="ac-inner ac-instant">
        <div>
          <span class="ac-instant-tag">💳 מוכנים להעמיק?</span>
          <h3>אפשר להשלים את ההרשמה והתשלום כאן ועכשיו</h3>
          <p>תשלום מאובטח בכרטיס אשראי. עד 40 מקומות בלבד לקבוצה.</p>
        </div>
        <a href={PAYMENT_URL} target="_blank" rel="noopener" class="ac-btn ac-btn-light">לתשלום מאובטח — 3,800 ₪ ←</a>
      </div>
    </section>

    <!-- FAQ -->
    <section class="ac-section ac-section-alt reveal">
      <div class="ac-inner ac-faq-inner">
        <div class="ac-eyebrow">שאלות נפוצות</div>
        <h2>יש לכם שאלות?</h2>
        <div class="ac-faq-list">
          {faqs.map((f) => (
            <details class="ac-faq-item">
              <summary>{f.q}<span class="ac-faq-plus" aria-hidden="true">+</span></summary>
              <p>{f.a}</p>
              {f.extra && (
                <div class="ac-faq-addendum">
                  <h4>{f.extra.title}</h4>
                  <p>{f.extra.intro}</p>
                  <ul class="ac-faq-contacts">
                    {f.extra.contacts.map((c) => <li>{c}</li>)}
                  </ul>
                  <ol class="ac-faq-rules">
                    {f.extra.rules.map((r) => <li>{r}</li>)}
                  </ol>
                  <p class="ac-faq-fine">{f.extra.outro}</p>
                </div>
              )}
            </details>
          ))}
        </div>
      </div>
    </section>

    <!-- REGISTRATION -->
    <section class="ac-section ac-register-section reveal" id="register">
      <div class="ac-register-shapes" aria-hidden="true">
        <span class="ac-shape ac-shape-diamond3"></span>
      </div>
      <div class="ac-inner">
        <div class="ac-form-wrap">
          <div class="ac-eyebrow ac-center">הרשמה לקורס</div>
          <h2 class="ac-center">הצטרפו לקורס<br />שפת גוף - מתקדמים</h2>
          <p class="ac-center">קורס ההמשך הבלעדי לבוגרים. עד 40 מקומות בלבד.</p>
          <form name="advanced-course-registration" method="POST" data-netlify="true" netlify-honeypot="bot-field">
            <input type="hidden" name="form-name" value="advanced-course-registration" />
            <p class="ac-hidden-field"><label>אל תמלאו שדה זה: <input name="bot-field" /></label></p>
            <div class="ac-form-row">
              <label>שם מלא<input type="text" name="name" required /></label>
              <label>מייל<input type="email" name="email" required /></label>
            </div>
            <label>טלפון<input type="tel" name="phone" required /></label>
            <button type="submit">אני רוצה להעמיק בשפת הגוף! ←</button>
          </form>
          <div class="ac-form-success" hidden>
            <p>נרשמת בהצלחה! נחזור אליך בהקדם 🎉</p>
            <p class="ac-pay-now">רוצים להבטיח את המקום כבר עכשיו?</p>
            <a href={PAYMENT_URL} target="_blank" rel="noopener" class="ac-btn">לתשלום מאובטח — 3,800 ₪ ←</a>
          </div>
          <p class="ac-form-error" hidden>אירעה תקלה בשליחה. נסו שוב או צרו קשר ישירות.</p>
          <p class="ac-disclaimer">מחיר מיוחד 3,800 ₪ בלבד (במקום 4,400 ₪). נחזור אליכם בהקדם.</p>
        </div>
      </div>
    </section>

    <Footer />
    <FloatingWhatsApp message="היי מעיין, אני בוגר/ת הקורס הבסיסי ומעוניין/ת בקורס המתקדמים (ינואר 2027) ואשמח לפרטים נוספים" />

    <script is:inline>
      const form = document.querySelector('form[name="advanced-course-registration"]');
      const submitButton = form?.querySelector('button[type="submit"]');
      const successMessage = document.querySelector('.ac-form-success');
      const errorMessage = document.querySelector('.ac-form-error');
      let isSubmitting = false;
      form?.addEventListener('submit', (event) => {
        event.preventDefault();
        if (isSubmitting) return;
        isSubmitting = true;
        if (submitButton) {
          submitButton.disabled = true;
          submitButton.dataset.originalText = submitButton.textContent ?? '';
          submitButton.textContent = 'שולח...';
        }
        errorMessage?.setAttribute('hidden', '');
        const formData = new FormData(form);
        const encode = Array.from(formData.entries())
          .map(([key, value]) => encodeURIComponent(key) + '=' + encodeURIComponent(String(value)))
          .join('&');

        const netlifyForms = fetch('/', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: encode,
        }).then((r) => r.ok);

        const crm = fetch('/api/advanced-course-lead', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            name: formData.get('name'),
            email: formData.get('email'),
            phone: formData.get('phone'),
          }),
        }).then((r) => r.ok).catch(() => false);

        Promise.allSettled([netlifyForms, crm]).then((results) => {
          const anyOk = results.some((r) => r.status === 'fulfilled' && r.value === true);
          if (anyOk) {
            form.hidden = true;
            successMessage?.removeAttribute('hidden');
          } else {
            errorMessage?.removeAttribute('hidden');
            isSubmitting = false;
            if (submitButton) {
              submitButton.disabled = false;
              submitButton.textContent = submitButton.dataset.originalText ?? submitButton.textContent;
            }
          }
        });
      });

      if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('in-view');
                observer.unobserve(entry.target);
              }
            });
          },
          { threshold: 0.01, rootMargin: '0px 0px -10% 0px' }
        );
        document.querySelectorAll('.reveal').forEach((el) => observer.observe(el));
      } else {
        document.querySelectorAll('.reveal').forEach((el) => el.classList.add('in-view'));
      }
    </script>
  </body>
</html>

<style>
  /* ===== navy & gold tokens ===== */
  :root {
    --ac-bg: #0b1024;
    --ac-bg-alt: #121a38;
    --ac-card: #17204a;
    --ac-ink: #f3efe3;
    --ac-ink-dim: rgba(243, 239, 227, 0.72);
    --ac-navy-deep: #0a0e1c;
    --ac-gold: #c9a05c;
    --ac-gold-soft: #e6cd94;
    --ac-gold-deep: #8a6423;
    --ac-rose: #dd7f93;
    --ac-border: rgba(201, 160, 92, 0.28);
    --ac-font-display: 'Assistant', sans-serif;
    --ac-font-label: 'Assistant', sans-serif;
    --ac-font-body: 'Assistant', sans-serif;
    --ac-shadow-soft: 0 14px 36px rgba(0, 0, 0, 0.35);
    --ac-shadow-soft-lg: 0 22px 55px rgba(0, 0, 0, 0.5);
  }

  body { background: var(--ac-bg); color: var(--ac-ink); }
  h1, h2, h3 { font-family: var(--ac-font-display); font-weight: 700; letter-spacing: -0.01em; }

  .ac-marker {
    font-style: normal;
    font-weight: 800;
    color: var(--ac-navy-deep);
    background: var(--ac-gold-soft);
    border-radius: 0.15em;
    padding: 0.02em 0.15em;
    box-decoration-break: clone;
    -webkit-box-decoration-break: clone;
  }
  .ac-marker-soft {
    font-style: normal;
    color: var(--ac-gold);
  }
  .ac-marker-strike {
    font-style: normal;
    position: relative;
    color: rgba(243, 239, 227, 0.4);
  }
  .ac-marker-strike::after {
    content: '';
    position: absolute;
    inset-inline: -0.1rem;
    top: 52%;
    height: 1.5px;
    background: var(--ac-rose);
  }

  /* ===== nav ===== */
  .ac-nav {
    position: sticky;
    top: 0;
    z-index: 100;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.85rem 2rem;
    background: rgba(11, 16, 36, 0.88);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--ac-border);
  }
  .ac-logo img { height: 26px; width: auto; }
  .ac-nav-cta {
    background: var(--ac-gold);
    color: var(--ac-navy-deep);
    font-family: var(--ac-font-label);
    font-weight: 700;
    font-size: 0.9rem;
    padding: 0.5rem 1.25rem;
    border-radius: 999px;
    text-decoration: none;
    white-space: nowrap;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }
  .ac-nav-cta:hover { transform: translateY(-2px); box-shadow: 0 8px 18px rgba(201, 160, 92, 0.3); }

  /* ===== hero ===== */
  .ac-hero {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    padding: 4rem 3rem 3.5rem;
    background:
      radial-gradient(ellipse 900px 600px at 88% 10%, rgba(201, 160, 92, 0.22), transparent 65%),
      radial-gradient(ellipse 700px 500px at 5% 100%, rgba(221, 127, 147, 0.12), transparent 60%),
      var(--ac-bg);
    color: var(--ac-ink);
    overflow: hidden;
  }
  .ac-shape { position: absolute; pointer-events: none; }
  .ac-shape-diamond1 {
    inset-inline-start: -60px;
    top: -60px;
    width: 200px;
    height: 200px;
    border: 1.5px solid var(--ac-border);
    transform: rotate(45deg);
  }
  .ac-shape-diamond2 {
    inset-inline-end: -40px;
    bottom: -80px;
    width: 220px;
    height: 220px;
    border: 1.5px dashed rgba(221, 127, 147, 0.25);
    transform: rotate(45deg);
  }
  .ac-hero-motif {
    position: absolute;
    inset-inline-end: 4%;
    top: 8%;
    width: 180px;
    height: 180px;
    color: var(--ac-gold);
    opacity: 0.35;
  }
  @media (max-width: 860px) { .ac-hero-motif { display: none; } }

  .ac-section-motif { position: relative; overflow: hidden; }
  .ac-motif { position: absolute; color: var(--ac-gold); opacity: 0.16; pointer-events: none; }
  .ac-motif-eye { width: 220px; height: auto; inset-inline-start: -2%; top: 12%; }
  .ac-motif-brain { width: 170px; height: auto; inset-inline-end: 2%; bottom: 8%; }
  .ac-practice-section { position: relative; overflow: hidden; }
  .ac-motif-body { width: 130px; height: auto; inset-inline-start: 2%; top: 6%; }
  .ac-motif-speech { width: 200px; height: auto; inset-inline-end: -1%; bottom: 4%; }
  @media (max-width: 860px) { .ac-motif { display: none; } }

  .ac-hero-text { max-width: 580px; position: relative; z-index: 2; }
  .ac-badge {
    display: inline-block;
    font-family: var(--ac-font-label);
    font-weight: 800;
    font-size: 1.15rem;
    background: var(--ac-gold);
    color: var(--ac-navy-deep);
    padding: 0.55rem 1.4rem;
    border-radius: 999px;
    margin-bottom: 1rem;
  }
  .ac-eyebrow-pill {
    display: block;
    font-family: var(--ac-font-label);
    color: var(--ac-gold-soft);
    font-size: 1.2rem;
    font-weight: 800;
    margin-bottom: 1.5rem;
    letter-spacing: 0.03em;
  }
  .ac-top-strip {
    text-align: center;
    padding: 2rem 1.5rem 0.5rem;
    background: var(--ac-bg);
  }
  .ac-top-strip .ac-badge { margin-bottom: 0.6rem; }
  .ac-top-strip .ac-eyebrow-pill { margin-bottom: 0; }
  :where(.ac-hero) h1 {
    font-size: clamp(2.4rem, 5.5vw, 3.9rem);
    line-height: 1.18;
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 1.25rem;
    color: var(--ac-ink);
  }
  .ac-hero-sub { font-family: var(--ac-font-body); font-size: 1.1rem; margin-bottom: 1.75rem; color: var(--ac-ink-dim); }
  .ac-bullets { list-style: none; padding: 0; margin: 0 0 2rem; display: grid; gap: 0.7rem; font-family: var(--ac-font-label); }
  .ac-bullets li { padding-inline-start: 1.9rem; position: relative; font-weight: 500; }
  .ac-bullets li::before {
    content: '›';
    position: absolute;
    inset-inline-start: 0;
    color: var(--ac-navy-deep);
    background: var(--ac-gold);
    width: 1.25rem;
    height: 1.25rem;
    border-radius: 50%;
    font-size: 0.9rem;
    font-weight: 900;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    top: 0.15rem;
  }
  .ac-cta-row { display: flex; align-items: center; gap: 1.75rem; flex-wrap: wrap; }

  .ac-btn {
    display: inline-block;
    background: linear-gradient(135deg, var(--ac-gold-soft), var(--ac-gold) 60%, var(--ac-gold-deep));
    color: var(--ac-navy-deep);
    font-family: var(--ac-font-label);
    font-weight: 800;
    font-size: 1.02rem;
    letter-spacing: 0.01em;
    padding: 0.9rem 2rem;
    border-radius: 999px;
    text-decoration: none;
    white-space: nowrap;
    box-shadow: var(--ac-shadow-soft), 0 0 0 1px rgba(201, 160, 92, 0.3) inset;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }
  .ac-btn:hover { transform: translateY(-2px); box-shadow: var(--ac-shadow-soft-lg), 0 0 24px rgba(201, 160, 92, 0.45); }

  .ac-price-tag { display: flex; flex-direction: column; line-height: 1.4; font-family: var(--ac-font-label); }
  .ac-price-old { text-decoration: line-through; opacity: 0.5; font-size: 0.9rem; }
  .ac-price-tag strong { color: var(--ac-rose); font-size: 1.25rem; }
  .ac-price-tag span:last-child { font-size: 0.85rem; opacity: 0.65; color: var(--ac-ink-dim); }

  .ac-hero-text-solo { max-width: 640px; margin: 0 auto; }
  .ac-hero:has(.ac-hero-text-solo) { justify-content: center; }

  .ac-practice-top { display: flex; align-items: center; gap: 3rem; margin-bottom: 3rem; text-align: start; }
  .ac-practice-photo { position: relative; z-index: 2; flex: 0 0 280px; width: 280px; }
  .ac-practice-photo-frame {
    position: absolute;
    inset: -14px;
    border: 1.5px solid rgba(201, 160, 92, 0.5);
    border-radius: 50%;
  }
  .ac-practice-photo::before {
    content: '';
    position: absolute;
    inset: 0;
    margin: auto;
    width: min(280px, 100%);
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(circle at 50% 32%, #1a2242, #080b17 88%);
    z-index: 0;
  }
  .ac-practice-photo img {
    max-width: min(280px, 100%);
    position: relative;
    z-index: 1;
    border-radius: 50%;
    box-shadow: var(--ac-shadow-soft-lg);
  }

  /* ===== trust bar ===== */
  .ac-trust-bar {
    display: flex;
    justify-content: center;
    gap: 3.5rem;
    flex-wrap: wrap;
    padding: 1.5rem 2rem;
    background: var(--ac-bg-alt);
    color: var(--ac-ink);
    font-weight: 600;
    font-family: var(--ac-font-label);
  }
  .ac-trust-bar div { display: flex; align-items: baseline; gap: 0.5rem; }
  .ac-trust-bar span {
    font-family: var(--ac-font-display);
    font-weight: 800;
    font-size: 1.9rem;
    color: var(--ac-gold-soft);
  }

  /* ===== sections ===== */
  .ac-section { padding: 4.5rem 2rem; background: var(--ac-bg); }
  .ac-section-alt { background: var(--ac-bg-alt); }
  .ac-inner { max-width: 1000px; margin: 0 auto; }
  .ac-eyebrow {
    font-family: var(--ac-font-label);
    font-weight: 800;
    color: var(--ac-rose);
    font-size: 1.3rem;
    letter-spacing: 0.04em;
    margin-bottom: 0.7rem;
    display: block;
  }
  :where(.ac-section) h2 {
    color: var(--ac-ink);
    font-size: clamp(2.1rem, 4.5vw, 2.9rem);
    margin-bottom: 0.85rem;
  }
  .ac-section-desc { margin-bottom: 2.25rem; max-width: 640px; font-family: var(--ac-font-body); color: var(--ac-ink-dim); }
  .ac-center { text-align: center; }
  .ac-center .ac-section-desc, .ac-center h2 { margin-inline: auto; }

  .reveal { opacity: 0; transform: translateY(26px); transition: opacity 0.7s ease, transform 0.7s ease; }
  .reveal.in-view { opacity: 1; transform: none; }

  /* ===== practice emphasis ===== */
  .ac-practice-text { flex: 1 1 480px; }
  .ac-practice-lead {
    margin: 0 0 1.25rem;
    font-family: var(--ac-font-body);
    font-size: 1.1rem;
    line-height: 1.8;
    color: var(--ac-ink-dim);
  }
  .ac-practice-note {
    margin: 0;
    font-family: var(--ac-font-label);
    font-weight: 700;
    font-size: 0.98rem;
    color: var(--ac-gold-soft);
  }
  .ac-practice-grid {
    display: flex;
    align-items: stretch;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .ac-practice-card {
    flex: 1 1 220px;
    max-width: 260px;
    background: var(--ac-card);
    border: 1px solid var(--ac-border);
    border-radius: 14px;
    padding: 1.75rem 1.5rem;
    box-shadow: var(--ac-shadow-soft);
  }
  .ac-practice-card-gold {
    background: linear-gradient(165deg, rgba(201, 160, 92, 0.28), var(--ac-card));
    border-color: var(--ac-gold);
  }
  .ac-practice-num {
    display: block;
    font-family: var(--ac-font-display);
    font-weight: 800;
    font-size: 1.3rem;
    color: var(--ac-gold);
    margin-bottom: 0.5rem;
  }
  .ac-practice-card h3 { font-size: 1.15rem; margin-bottom: 0.5rem; color: var(--ac-ink); }
  .ac-practice-card p { font-family: var(--ac-font-body); font-size: 0.92rem; margin: 0; color: var(--ac-ink-dim); }
  .ac-practice-arrow { align-self: center; font-size: 1.4rem; color: var(--ac-gold); opacity: 0.7; }

  /* ===== modules ===== */
  .ac-modules { display: grid; gap: 1.25rem; margin-top: 2.5rem; }
  .ac-module {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
    background: var(--ac-card);
    border: 1px solid var(--ac-border);
    border-radius: 14px;
    padding: 1.5rem 1.75rem;
    box-shadow: var(--ac-shadow-soft);
  }
  .ac-module-num {
    flex-shrink: 0;
    font-family: var(--ac-font-display);
    font-weight: 800;
    font-size: 2.1rem;
    color: var(--ac-gold);
    line-height: 1;
  }
  .ac-module-body h3 { font-family: var(--ac-font-label); color: var(--ac-ink); font-size: 1.1rem; margin-bottom: 0.4rem; font-weight: 700; }
  .ac-module-body p { margin: 0; line-height: 1.7; font-family: var(--ac-font-body); font-size: 0.95rem; color: var(--ac-ink-dim); }
  .ac-module-body strong { color: var(--ac-gold-soft); }

  /* ===== guest photos ===== */
  .ac-guest-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; align-items: center; }
  .ac-guest-photos { position: relative; justify-self: center; width: 100%; max-width: 380px; }
  .ac-guest-photo-main {
    width: 100%;
    border-radius: 16px;
    box-shadow: var(--ac-shadow-soft-lg);
    position: relative;
    z-index: 1;
  }
  .ac-guest-photo-accent {
    position: absolute;
    width: 46%;
    bottom: -12%;
    inset-inline-start: -12%;
    border-radius: 12px;
    border: 4px solid var(--ac-bg);
    box-shadow: var(--ac-shadow-soft);
    z-index: 2;
  }
  .ac-guest-text p { font-family: var(--ac-font-body); line-height: 1.85; color: var(--ac-ink-dim); }

  /* ===== schedule tickets ===== */
  .ac-tickets { display: flex; flex-wrap: wrap; gap: 1.25rem; margin-bottom: 2.5rem; }
  .ac-ticket {
    display: flex;
    flex-direction: column;
    align-items: center;
    background: var(--ac-card);
    border: 1.5px dashed var(--ac-gold);
    border-radius: 12px;
    padding: 0.9rem 1.4rem;
    box-shadow: var(--ac-shadow-soft);
  }
  .ac-ticket-day { font-family: var(--ac-font-label); font-size: 0.8rem; font-weight: 600; color: var(--ac-rose); }
  .ac-ticket-date {
    font-family: var(--ac-font-display);
    font-weight: 800;
    font-size: 1.7rem;
    color: var(--ac-gold-soft);
    line-height: 1.2;
  }
  .ac-ticket-num { font-family: var(--ac-font-label); font-size: 0.75rem; opacity: 0.7; color: var(--ac-ink-dim); }

  .ac-meta-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1.25rem; font-family: var(--ac-font-body); }
  .ac-meta-item { display: flex; gap: 0.65rem; align-items: flex-start; }
  .ac-meta-item h4 { font-family: var(--ac-font-label); margin: 0 0 0.15rem; color: var(--ac-ink); }
  .ac-meta-item p { margin: 0; font-size: 0.95rem; color: var(--ac-ink-dim); }
  .ac-emoji { font-size: 1.3rem; }
  .ac-schedule-note {
    margin: 1.75rem 0 0;
    font-family: var(--ac-font-label);
    font-weight: 600;
    font-size: 0.95rem;
    color: var(--ac-gold-soft);
  }

  /* ===== early bird banner ===== */
  .ac-early-bird-banner {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    background: linear-gradient(120deg, rgba(201, 160, 92, 0.22), var(--ac-card));
    border: 1.5px solid var(--ac-gold);
    border-radius: 16px;
    padding: 1.5rem 2rem;
    margin-bottom: 2.5rem;
  }
  .ac-early-bird-icon { font-size: 2.2rem; flex-shrink: 0; }
  .ac-early-bird-banner strong {
    display: block;
    font-family: var(--ac-font-display);
    font-weight: 800;
    font-size: 1.35rem;
    color: var(--ac-gold-soft);
    margin-bottom: 0.35rem;
  }
  .ac-early-bird-banner p {
    margin: 0;
    font-family: var(--ac-font-body);
    color: var(--ac-ink-dim);
    font-size: 0.98rem;
  }

  /* ===== price ===== */
  .ac-price-grid { display: grid; grid-template-columns: 1.2fr 1fr; gap: 2rem; align-items: start; }
  .ac-price-card {
    background: var(--ac-card);
    border: 1px solid var(--ac-border);
    border-radius: 16px;
    padding: 2.25rem;
    box-shadow: var(--ac-shadow-soft-lg);
  }
  .ac-price-label { font-family: var(--ac-font-label); font-weight: 700; color: var(--ac-rose); margin-bottom: 0.5rem; }
  .ac-price-was { text-decoration: line-through; opacity: 0.5; font-family: var(--ac-font-label); color: var(--ac-ink-dim); }
  .ac-price-now {
    font-family: var(--ac-font-display);
    font-weight: 800;
    font-size: 4rem;
    color: var(--ac-gold-soft);
    line-height: 1.1;
    margin-bottom: 0.5rem;
  }
  .ac-price-now span { font-size: 1.8rem; }
  .ac-price-card p { font-family: var(--ac-font-body); color: var(--ac-ink-dim); }
  .ac-price-card .ac-btn { margin-top: 1.5rem; }
  .ac-urgency {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 1.25rem;
    color: var(--ac-rose);
    font-weight: 700;
    font-size: 0.9rem;
    font-family: var(--ac-font-label);
  }
  .ac-urgency-dot { width: 0.5rem; height: 0.5rem; border-radius: 50%; background: var(--ac-rose); animation: ac-pulse 1.4s infinite; }
  @keyframes ac-pulse { 50% { opacity: 0.3; } }
  .ac-info-card {
    background: var(--ac-bg-alt);
    border: 1.5px dashed var(--ac-gold);
    border-radius: 16px;
    padding: 2rem;
  }
  .ac-info-photo {
    display: block;
    width: 72px;
    height: 72px;
    border-radius: 50%;
    overflow: hidden;
    background: var(--ac-navy-deep);
    border: 2px solid var(--ac-gold);
    margin-bottom: 0.85rem;
  }
  .ac-info-photo img { width: 100%; height: 100%; object-fit: cover; object-position: center 12%; display: block; }
  .ac-info-tag { font-family: var(--ac-font-label); font-weight: 700; color: var(--ac-gold); }
  .ac-info-card h3 { color: var(--ac-ink); margin: 0.5rem 0; font-size: 1.25rem; }
  .ac-info-card p { font-family: var(--ac-font-body); font-size: 0.92rem; color: var(--ac-ink-dim); }
  .ac-info-card hr { border: none; border-top: 1px dashed rgba(201, 160, 92, 0.35); margin: 1.25rem 0; }
  .ac-small { font-size: 0.85rem; opacity: 0.75; font-family: var(--ac-font-body); color: var(--ac-ink-dim); }

  /* ===== payment banner ===== */
  .ac-instant-section {
    background: linear-gradient(120deg, var(--ac-gold-soft), var(--ac-rose));
  }
  .ac-instant { display: flex; align-items: center; justify-content: space-between; gap: 2rem; color: var(--ac-navy-deep); font-family: var(--ac-font-body); }
  .ac-instant-tag { font-family: var(--ac-font-label); font-weight: 700; color: var(--ac-navy-deep); }
  .ac-instant h3 { font-size: 1.5rem; margin: 0.5rem 0; color: var(--ac-navy-deep); }
  .ac-instant p { margin: 0; opacity: 0.8; }
  .ac-btn-light { flex-shrink: 0; background: var(--ac-navy-deep); color: var(--ac-gold-soft); }

  /* ===== faq ===== */
  .ac-faq-inner { max-width: 760px; }
  .ac-faq-list { display: grid; }
  .ac-faq-item { border-bottom: 1px solid var(--ac-border); padding: 0.35rem 0; }
  .ac-faq-item summary {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    font-family: var(--ac-font-label);
    font-weight: 700;
    font-size: 1.02rem;
    cursor: pointer;
    color: var(--ac-ink);
    padding: 0.85rem 0;
    list-style: none;
  }
  .ac-faq-item summary::-webkit-details-marker { display: none; }
  .ac-faq-plus {
    flex-shrink: 0;
    width: 1.75rem;
    height: 1.75rem;
    border: 1.5px solid var(--ac-gold);
    border-radius: 50%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: var(--ac-gold);
    font-weight: 700;
    transition: transform 0.2s ease;
  }
  .ac-faq-item[open] .ac-faq-plus { transform: rotate(45deg); background: var(--ac-gold); color: var(--ac-navy-deep); }
  .ac-faq-item p { margin: 0 0 1rem; line-height: 1.7; font-family: var(--ac-font-body); color: var(--ac-ink-dim); }

  .ac-faq-addendum {
    margin: 0.25rem 0 1.25rem;
    padding: 1.5rem 1.5rem 1.25rem;
    background: var(--ac-card);
    border-inline-start: 2px dashed var(--ac-rose);
    border-radius: 4px;
    font-family: var(--ac-font-body);
  }
  .ac-faq-addendum h4 {
    font-family: var(--ac-font-label);
    font-weight: 700;
    color: var(--ac-gold);
    font-size: 1rem;
    margin: 0 0 0.75rem;
  }
  .ac-faq-addendum > p { font-size: 0.92rem; margin-bottom: 0.75rem; }
  .ac-faq-contacts {
    list-style: none;
    padding: 0;
    margin: 0 0 1rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }
  .ac-faq-contacts li {
    font-weight: 700;
    font-size: 0.88rem;
    color: var(--ac-ink);
    background: var(--ac-bg-alt);
    border: 1px solid var(--ac-gold);
    border-radius: 999px;
    padding: 0.3rem 0.9rem;
  }
  .ac-faq-rules {
    margin: 0 0 1rem;
    padding-inline-start: 1.3rem;
    display: grid;
    gap: 0.6rem;
  }
  .ac-faq-rules li { font-size: 0.92rem; line-height: 1.6; color: var(--ac-ink-dim); }
  .ac-faq-rules li::marker { font-weight: 800; color: var(--ac-rose); }
  .ac-faq-fine { font-size: 0.78rem; opacity: 0.7; line-height: 1.6; margin: 0; color: var(--ac-ink-dim); }

  /* ===== register ===== */
  .ac-register-section { position: relative; background: var(--ac-bg-alt); overflow: hidden; }
  .ac-shape-diamond3 {
    inset-inline-end: -80px;
    bottom: -80px;
    width: 220px;
    height: 220px;
    border: 1.5px dashed var(--ac-border);
    transform: rotate(45deg);
  }
  .ac-form-wrap {
    position: relative;
    max-width: 580px;
    margin: 0 auto;
    background: var(--ac-card);
    border: 1px solid var(--ac-border);
    border-radius: 20px;
    padding: 2.75rem 2.25rem;
    box-shadow: var(--ac-shadow-soft-lg);
  }
  .ac-form-wrap form { display: flex; flex-direction: column; gap: 1.1rem; margin-top: 1.75rem; font-family: var(--ac-font-body); }
  .ac-form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1.1rem; }
  .ac-form-wrap label { display: flex; flex-direction: column; gap: 0.35rem; font-weight: 700; font-size: 0.95rem; font-family: var(--ac-font-label); color: var(--ac-ink); }
  .ac-form-wrap input {
    padding: 0.7rem;
    border-radius: 10px;
    border: 1.5px solid rgba(243, 239, 227, 0.2);
    font-family: inherit;
    font-size: 1rem;
    background: rgba(255, 255, 255, 0.06);
    color: var(--ac-ink);
    transition: border-color 0.15s ease;
  }
  .ac-form-wrap input:focus { outline: none; border-color: var(--ac-gold); }
  .ac-form-wrap button {
    font-family: var(--ac-font-label);
    padding: 1rem;
    background: var(--ac-gold);
    color: var(--ac-navy-deep);
    border: none;
    border-radius: 999px;
    font-weight: 700;
    font-size: 1.1rem;
    cursor: pointer;
    box-shadow: var(--ac-shadow-soft);
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }
  .ac-form-wrap button:hover { transform: translateY(-2px); box-shadow: var(--ac-shadow-soft-lg); }
  .ac-hidden-field { display: none; }
  .ac-form-success, .ac-form-error { text-align: center; font-weight: 700; margin-top: 1.25rem; font-family: var(--ac-font-label); }
  .ac-form-success { color: var(--ac-gold-soft); }
  .ac-form-success p { margin-bottom: 0.85rem; }
  .ac-pay-now { color: var(--ac-ink); font-weight: 600; }
  .ac-form-error { color: var(--ac-rose); }
  [hidden] { display: none; }
  .ac-disclaimer { text-align: center; font-size: 0.85rem; opacity: 0.65; margin-top: 1.25rem; font-family: var(--ac-font-body); color: var(--ac-ink-dim); }

  /* ===== responsive ===== */
  @media (max-width: 860px) {
    .ac-hero { flex-direction: column; text-align: center; padding: 3.5rem 1.5rem 3rem; }
    .ac-bullets li { text-align: right; }
    .ac-cta-row { justify-content: center; }
    .ac-price-grid { grid-template-columns: 1fr; }
    .ac-instant { flex-direction: column; text-align: center; }
    .ac-early-bird-banner { flex-direction: column; text-align: center; }
    .ac-form-row { grid-template-columns: 1fr; }
    .ac-trust-bar { gap: 1.5rem; }
    .ac-tickets { justify-content: center; }
    .ac-module { flex-direction: column; gap: 0.5rem; }
    .ac-practice-top { flex-direction: column; text-align: center; }
    .ac-practice-grid { flex-direction: column; align-items: center; }
    .ac-practice-arrow { transform: rotate(-90deg); }
    .ac-guest-grid { grid-template-columns: 1fr; }
    .ac-guest-photos { margin-bottom: 2.5rem; }
  }
</style>
