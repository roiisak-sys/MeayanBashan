import { defineConfig } from 'astro/config';

export default defineConfig({
  // Placeholder — a later task replaces this with the real domain once DNS is connected.
  site: 'https://maayanbashan.co.il',
});
